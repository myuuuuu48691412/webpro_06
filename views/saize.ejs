<!Doctype html>
<html>
<head>
    <meta charset="utf-8">
    <title>サイゼのメニュー</title>
</head>
<body>
    <h2>サイゼリヤ メニュー一覧</h2>
<p><a href="/saize/new">＋ 新規メニュー追加</a></p>
    <table>
        <tr><th>ID</th><th>商品名</th></tr>
            <% let number = 0 %>
            <% for( let row of data ) { %>
                <tr><td><%= row.id %></td>
                <td><a href="/saize/<%= number %>"><%= row.name %></a></td></tr>
                <% number += 1 %>
            <% } %>
    </table>
</body>
</html>